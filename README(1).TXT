The file to store the hash data is python dictionary that is stored with json.

The key to the dictionary is the absolute filepath the hash and date/time are the 2 values associated with it.

The script takes in 2 inputs. 

The first input is the filename to store the hashes.

The second input is optional and is a previous output of hashes from the program. It is necessary to see the differences between the current filesystem and past file system.

The extra credit was completed.
